import datetime
from logstown import db

SPONSOR = "UPDATE users SET sponsor_since=%s WHERE email=%s"

# ========================================================================== ^L

email = request.user.session['email']
today = datetime.date.today()
db.execute(SPONSOR, (today, email))
response.body = {"problem": "", "redirect": "/sponsorship/"}
