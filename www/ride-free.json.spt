from aspen import Response
from gratipay.utils import get_participant

[-----------------------------------------------------------------------------]

if user.ANON:
    raise Response(403)
request.allow("POST")
user.participant.update_is_free_rider(True)
raise Response(204)

[---] application/json via json_dump
