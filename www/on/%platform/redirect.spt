import os
from aspen import Response

[-----------------------------]

platform = website.platforms[path['platform']]

host = os.getenv('CANONICAL_HOST')
scheme = os.getenv('CANONICAL_SCHEME')

redirect_uri = '%s://%s%s' % (
    scheme,
    host,
    '/on/%s/associate' % platform.name
)

request.redirect(platform.get_oauth_init_url(redirect_uri, qs, website))


[-----------------------------] text/plain
