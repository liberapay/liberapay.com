from aspen import Response
^L
if request.user.ANON:
    raise Response(403)

if not request.POST:
    raise Response(400)

problem = ""

name = request.body.one('class-name').strip()
what = request.body.one('what').strip()
when = request.body.one('when').strip()
where = request.body.one('where').strip()

response.body = {"problem": problem}
