from liberapay.utils import get_participant
[---]
participant = get_participant(state, restrict=True, allow_member=True)
response.redirect(participant.path('edit/avatar'), code=307)
[---] text/html

[---] application/json
