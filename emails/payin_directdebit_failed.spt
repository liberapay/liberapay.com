{{ _("The direct debit payment to your Liberapay wallet failed") }}

[---] text/html
<p>{{ _(
    "The attempt to transfer {0} from your bank account failed because:",
    LegacyMoney(exchange.amount + exchange.fee)
) }}</p>

<pre>{{ exchange.note }}</pre>

<p>{{ _("You can try again, possibly with another payment method, by clicking the link below:") }}</p>

<p><a href="{{ participant.url('wallet/payin/') }}">{{ _("Try again") }}</a></p>
